---
interface Props {
    id: string;
    liveSlug: string | null;
    locale: string;
}

const { id, liveSlug, locale } = Astro.props;
---

<div class="mb-4 rounded-md border border-amber-300 bg-amber-50 px-4 py-2 text-sm text-amber-800">
    Preview mode — page id <code>{id}</code>{liveSlug && <> · live slug: <code>/{locale}/{liveSlug}</code></>}
</div>
