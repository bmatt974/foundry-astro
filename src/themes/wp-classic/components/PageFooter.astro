---
import type { PageTranslation } from '../../../lib/foundry';
import Markdown from './Markdown.astro';

interface Props {
    translation: PageTranslation;
}

const { translation: t } = Astro.props;
---

{t.conclusion && (
    <footer class="entry-footer">
        <Markdown text={t.conclusion} />
    </footer>
)}
